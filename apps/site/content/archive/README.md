# Archived MDX content (retired)

This archive and the pipeline behind it are retired; live content is authored as MDX under the repository-root `content/`.
This directory is deleted with `apps/site` when production points at `apps/web`.
