# `apps/site` — previous site (not deployed)

This is the old Next.js + Payload CMS app. It is **no longer deployed**.

The live site is [`apps/web/`](../web/). Do not add features here, and do not run its database or admin tooling. This directory stays in the tree until it is deleted as part of Phase 2 of [`docs/product/roadmap.md`](../../docs/product/roadmap.md).
