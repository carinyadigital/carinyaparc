# Archived MDX content (retired)

This archive and the pipeline behind it are retired by the Astro migration (`docs/astro-migration/PLAN.md`); live content is authored as MDX under the repository-root `content/`.
This directory is deleted with `apps/site` in Phase 7 of that plan.
