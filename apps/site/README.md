# `apps/site` — previous site (reference only)

This directory is the old Next.js + Payload CMS application. It is **not a workspace package**, **not deployed**, and **not built, linted or tested in CI**.

The live site is [`apps/web/`](../web/). Do not add features here, and do not run its database or admin tooling. The Vercel project that used to deploy this app has been deleted.

Kept in the repository only as a historical reference.
